Matrices- can only contain a single class of data 
Data Frames can consist of many different classes of data 

my_vector<-c(1:20)
dim(my_vector) <-c(4,5) 

attribute(my_vector) will call attribute of my_vector 

matrix is simply an atomic vector with a dimension attribute! 

matrix(data = NA, nrow = 1, ncol = 1, byrow = FALSE,
       dimnames = NULL)


If we want to bind different classes of data, use data.frame() 

colnames(my_data) <-yourcolname 
